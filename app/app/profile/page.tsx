import { Suspense } from 'react'
import { ProfileExperience } from '@/components/profile/ProfileExperience'

export default function OwnProfilePage() {
  return <Suspense><ProfileExperience /></Suspense>
}
